# Build-time PUBLIC values for `npm run cf:build`. Anything here ships in the
# client bundle — never put a secret in this file (secrets: `wrangler secret put`).
NEXT_PUBLIC_SITE_URL=https://fineartfree.com
